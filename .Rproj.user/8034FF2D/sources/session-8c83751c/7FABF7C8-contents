hourtest <- bike_df %>% 
  filter(trip_duration >= 30) %>% 
  group_by(member_casual, type_of_day) %>% 
  summarize(total = n(), .groups = "drop")

ggplot(data = hourtest, aes(x=member_casual, y=total)) + geom_bar(stat = "identity")

hourtest

rm(hourtest)
